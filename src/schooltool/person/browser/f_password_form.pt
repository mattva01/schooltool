<script>
  // XXX: we need a better way to do this
  //      can z3c.form set custom classes on error?
  $(document).ready(function() {
    $('form fieldset .row .error').prev().children(':first-child').addClass('error');
  });
</script>
<div metal:use-macro="macro:form" />
